#ifndef BK_PARTITION_TYPES_H
#define BK_PARTITION_TYPES_H

#include <iostream>
#include <vector>
#include <set>

using VertexType = unsigned int;
using EdgeType = std::pair<VertexType, VertexType>;
using CliqueType = std::vector<VertexType>;

#endif //BK_PARTITION_TYPES_H
